// import t from 'assert';
// import a from 'assertron';
// import { createSatisfier } from '.';

test.skip('extra entry in array should fail', () => {
  // const diff = createSatisfier(exact([1])).exec([1, 2])
  // t.strictEqual(diff!.length, 1)
  // a.satisfy(diff![0], { path: ['[1]'], expected: undefined, actual: 2 })
})

